
class Point {
    int x, y;

    Point(int x, int y) {
        this.x = x;
        this.y = y;
    }
}

class ClosestPair {

    // public static double closestPair(Point[] points) {
    // Arrays.sort(points, Comparator.comparingInt(p -> p.x));
    // // return closestPairRect(points, 0, points.length - 1);
    // }

}
